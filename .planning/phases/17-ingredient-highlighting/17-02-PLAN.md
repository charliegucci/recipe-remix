---
phase: 17-ingredient-highlighting
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - server/api/recipes/[id]/substitute.post.ts
  - server/utils/substitution-prompt.ts
  - app/components/SubstitutionDialog.vue
autonomous: true
requirements:
  - INGR-03
  - INGR-04

must_haves:
  truths:
    - "Substitution panel shows AI-suggested replacements that prioritize the user's actual pantry ingredients"
    - "User can manually pick any pantry item as a replacement from a scrollable list in the substitution panel"
    - "Selecting a manual replacement updates the ingredient display on the recipe without an AI call"
    - "AI suggestions note which suggestions come from the user's pantry vs general suggestions"
  artifacts:
    - path: "server/api/recipes/[id]/substitute.post.ts"
      provides: "Accepts optional pantryItems array to bias AI toward user's pantry"
      contains: "pantryItems"
    - path: "server/utils/substitution-prompt.ts"
      provides: "Prompt includes pantry context when available"
      contains: "pantry"
    - path: "app/components/SubstitutionDialog.vue"
      provides: "Two-tab substitution panel: AI suggestion + manual pantry pick"
      contains: "pantry"
  key_links:
    - from: "app/components/SubstitutionDialog.vue"
      to: "server/api/recipes/[id]/substitute.post.ts"
      via: "$fetch with pantryItems in body"
      pattern: "pantryItems"
    - from: "app/components/SubstitutionDialog.vue"
      to: "app/composables/usePantry.ts"
      via: "usePantry().pantry for manual pick list"
      pattern: "usePantry"
    - from: "server/utils/substitution-prompt.ts"
      to: "server/api/recipes/[id]/substitute.post.ts"
      via: "buildSubstitutionPrompt receives pantryItems param"
      pattern: "pantryItems"
---

<objective>
Build pantry-aware substitution with AI suggestions biased toward the user's pantry and a manual pantry-pick option.

Purpose: When a user taps a missing ingredient, they can either get an AI suggestion that prefers their pantry items or manually pick from their pantry — making substitution immediate and practical.

Output: Enhanced substitute API that accepts pantry context, updated prompt builder, and a two-mode SubstitutionDialog (AI suggest + manual pick).
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ingredient-highlighting/17-01-SUMMARY.md
@server/api/recipes/[id]/substitute.post.ts
@server/utils/substitution-prompt.ts
@server/utils/substitution-parser.ts
@app/components/SubstitutionDialog.vue
@app/composables/usePantry.ts
@app/pages/recipe/[slug].vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance substitute API and prompt to accept pantry context</name>
  <files>
    server/api/recipes/[id]/substitute.post.ts
    server/utils/substitution-prompt.ts
  </files>
  <action>
  **substitution-prompt.ts:**
  1. Add optional `pantryItems?: string[]` to `SubstitutionPromptParams`
  2. When `pantryItems` is provided and non-empty, add a section to the prompt:
     ```
     The user has the following items in their pantry:
     - {item1}
     - {item2}
     ...
     STRONGLY PREFER suggesting a substitute from the user's pantry items above. If a good match exists in the pantry, use it. If no pantry item is suitable, suggest the best general substitute and note it's not in the user's pantry.
     ```
  3. Add a new field to the expected JSON output: `"fromPantry": true/false` indicating whether the suggestion came from the user's pantry

  **substitute.post.ts:**
  1. Add optional `pantryItems?: string[]` to the `SubstituteRequest` interface
  2. Pass `pantryItems` through to `buildSubstitutionPrompt` when present
  3. No validation needed for pantryItems — it's optional and just an array of strings
  4. Keep backward compatibility: if `pantryItems` is not provided, the prompt works exactly as before

  **substitution-parser.ts:**
  Update `ParsedSubstitution` to include optional `fromPantry?: boolean`. Parse it from the response if present, default to `false` if missing.
  </action>
  <verify>
  Run `npx nuxi typecheck` to verify no type errors in server code. Verify the prompt builder produces correct output by reading the updated code.
  </verify>
  <done>
  Substitute API accepts optional `pantryItems` array, prompt biases AI toward pantry items when provided, response includes `fromPantry` flag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add manual pantry-pick and pantry-aware AI mode to SubstitutionDialog</name>
  <files>
    app/components/SubstitutionDialog.vue
  </files>
  <action>
  Redesign SubstitutionDialog to have two modes accessible via tabs or segmented control at the top of the dialog:

  **Tab 1: "AI Suggest" (default)**
  - Works like existing flow but now passes `pantryItems` (from `usePantry().pantry`) to the API
  - Import `usePantry` and get `pantry` computed
  - When calling `/api/recipes/:id/substitute`, include `pantryItems: pantry.value.map(p => p.name)` in the body
  - If result has `fromPantry: true`, show a green "From your pantry!" badge next to the substitute name
  - If `fromPantry: false`, show a subtle note: "Not in your pantry — you may need to buy this"
  - Keep existing reason selector, find button, result display, and accept/cancel buttons

  **Tab 2: "Pick from Pantry"**
  - Show a scrollable list of all pantry items (from `usePantry().pantry`)
  - Each item is a tappable row with the ingredient name
  - Add a search/filter input at the top to filter the pantry list (simple case-insensitive name match)
  - When user taps a pantry item, immediately emit `accept` with:
    - `substitute: { name: selectedItem.name, quantity: '(adjust to taste)', unit: '' }`
    - `updatedInstructions: []` (empty — no AI instruction update for manual pick)
    - `updatedExplanation: ''`
    - `substitutionNote: 'Manually selected from your pantry'`
  - If pantry is empty, show a message: "Your pantry is empty. Add ingredients on the Pantry page." with a link to /pantry

  **Tab styling:** Use a simple two-button segmented control at the top:
  - Active tab: `bg-indigo-600 text-white rounded-lg`
  - Inactive tab: `text-gray-600 hover:bg-gray-100 rounded-lg`

  **Handle the accept in [slug].vue:**
  The existing `handleSubstitutionAccepted` in [slug].vue already handles the result shape. For manual picks, `updatedInstructions` will be empty — update the handler to skip instruction replacement when `updatedInstructions` is empty (check `result.updatedInstructions.length > 0` before replacing, which it already does).
  </action>
  <verify>
  Run `npx nuxi typecheck` to verify no type errors. Run `npx nuxi build 2>&1 | tail -5` to confirm build succeeds.
  </verify>
  <done>
  SubstitutionDialog has two modes: AI Suggest (pantry-aware) and Pick from Pantry (manual). AI suggestions indicate whether the substitute is from the user's pantry. Manual pick immediately applies without AI call. Both modes emit the same `accept` event shape for compatibility with existing recipe detail page handler.
  </done>
</task>

</tasks>

<verification>
1. Open a recipe detail page with some pantry items set — missing ingredients show amber "Missing" (from 17-01)
2. Tap a missing ingredient — substitution dialog opens with two tabs
3. "AI Suggest" tab: click "Find Substitute" — result shows "From your pantry!" if the AI picked a pantry item
4. "Pick from Pantry" tab: shows list of user's pantry items, can filter, tapping one applies it as substitution
5. After accepting either AI or manual substitution, the ingredient display updates on the recipe page
6. No type errors, build succeeds
</verification>

<success_criteria>
- SubstitutionDialog sends pantry context to AI for smarter suggestions
- AI results flag whether suggestion is from pantry
- Manual pantry-pick works without AI call
- Both substitution modes update the recipe ingredient display correctly
- Existing substitution flow for non-pantry-aware usage still works (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/17-ingredient-highlighting/17-02-SUMMARY.md`
</output>
