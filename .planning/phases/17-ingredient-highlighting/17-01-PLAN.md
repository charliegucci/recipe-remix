---
phase: 17-ingredient-highlighting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/composables/usePantry.ts
  - app/components/IngredientChecklist.vue
  - app/pages/generate.vue
autonomous: true
requirements:
  - INGR-01
  - INGR-02

must_haves:
  truths:
    - "Ingredients not in the user's pantry are visually distinguished (amber/orange badge + background tint) on the recipe detail page"
    - "Ingredients that ARE in the user's pantry show a subtle green 'in pantry' indicator"
    - "On the generate result page, each ingredient shows whether it's in the user's pantry or missing"
    - "Tapping a missing ingredient on the recipe detail page opens the existing substitution dialog"
  artifacts:
    - path: "app/composables/usePantry.ts"
      provides: "isInPantryByName(name) helper for name-based pantry matching"
      contains: "isInPantryByName"
    - path: "app/components/IngredientChecklist.vue"
      provides: "Visual highlighting of missing vs pantry ingredients with tap-to-substitute on missing items"
      contains: "missing"
    - path: "app/pages/generate.vue"
      provides: "Ingredient list on generate result shows pantry status per ingredient"
      contains: "isInPantryByName"
  key_links:
    - from: "app/components/IngredientChecklist.vue"
      to: "app/composables/usePantry.ts"
      via: "usePantry().isInPantryByName(ingredient.name)"
      pattern: "isInPantryByName"
    - from: "app/pages/generate.vue"
      to: "app/composables/usePantry.ts"
      via: "usePantry().isInPantryByName(ingredient.name)"
      pattern: "isInPantryByName"
---

<objective>
Add missing-ingredient detection and visual highlighting to the recipe detail page (IngredientChecklist) and the generate result page.

Purpose: Users immediately see which recipe ingredients they already have and which they're missing, so they can plan shopping or swap ingredients.

Output: Updated IngredientChecklist with pantry-aware highlighting, updated generate page result section with pantry badges, and a new `isInPantryByName` helper in usePantry.
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/composables/usePantry.ts
@app/components/IngredientChecklist.vue
@app/pages/generate.vue
@app/pages/recipe/[slug].vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isInPantryByName helper and wire pantry highlighting into IngredientChecklist</name>
  <files>
    app/composables/usePantry.ts
    app/components/IngredientChecklist.vue
  </files>
  <action>
  **usePantry.ts:**
  Add a new function `isInPantryByName(ingredientName: string): boolean` that checks if any pantry item's `name` matches the given ingredient name using case-insensitive substring matching (same matching logic used in pantry-to-recipe matching from Phase 3 — check both directions: pantry item name includes ingredient name OR ingredient name includes pantry item name). Export it alongside existing returns.

  **IngredientChecklist.vue:**
  1. Import `usePantry` and call `const { isInPantryByName } = usePantry()` in setup
  2. For each ingredient in the list, compute whether it's in the pantry: `isInPantryByName(ingredient.name)`
  3. Visual treatment for **missing** ingredients (NOT in pantry):
     - Add an amber/orange left border or background tint: `border-l-4 border-amber-400 bg-amber-50/50`
     - Show a small "Missing" badge: `<span class="text-xs px-1.5 py-0.5 bg-amber-100 text-amber-700 rounded-full font-medium">Missing</span>` next to the ingredient name
     - The existing swap icon (already present for AI recipes) stays as-is — it already emits `substitute`
  4. Visual treatment for **pantry** ingredients (IS in pantry):
     - Show a subtle green checkmark icon or "In Pantry" badge: `<span class="text-xs px-1.5 py-0.5 bg-green-100 text-green-700 rounded-full font-medium">In Pantry</span>`
  5. For missing ingredients, make the ENTIRE row tappable to trigger substitution (not just the swap icon). On click of a missing ingredient row, if `isAiGenerated` is true, emit `substitute` with the ingredient name. If not AI-generated, the tap just toggles the checklist as before.
  6. Keep the existing checkbox toggle working for all ingredients — the missing/pantry badges are informational overlays, not replacements for the checklist.
  </action>
  <verify>
  Run `npx nuxi typecheck` to verify no type errors. Manually inspect IngredientChecklist to confirm it renders with pantry-aware badges.
  </verify>
  <done>
  IngredientChecklist shows "Missing" badge with amber styling for ingredients not in pantry, "In Pantry" badge with green styling for pantry ingredients. Tapping a missing ingredient on an AI recipe triggers the substitution dialog. Existing checkbox behavior is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pantry highlighting to generate page result ingredients</name>
  <files>
    app/pages/generate.vue
  </files>
  <action>
  In the generate result section (the `<ul>` that renders `generatedRecipe.ingredients`), add pantry-awareness:

  1. Import `usePantry` (already imported in generate.vue for `pantryState`) — reuse `pantryState` or call `isInPantryByName` from it
  2. For each ingredient `<li>`, add conditional styling:
     - If in pantry: green dot bullet instead of orange (`text-green-600`) and a small "Have it" or checkmark
     - If NOT in pantry: amber dot bullet (`text-amber-600`) and a small "Missing" text in amber
  3. Add a summary line above the ingredient list: "You have X of Y ingredients" with a visual bar or fraction
  4. Keep the layout clean — use small inline badges similar to IngredientChecklist

  The generate page result does NOT need substitution flow (the user can click "View Full Recipe" to access substitution on the detail page). This is informational only.
  </action>
  <verify>
  Run `npx nuxi typecheck` to verify no type errors. Build check: `npx nuxi build 2>&1 | tail -5` should show no errors.
  </verify>
  <done>
  Generate result page shows pantry-aware ingredient list with green/amber indicators and a "X of Y ingredients" summary line. Users immediately see what they have vs what they're missing.
  </done>
</task>

</tasks>

<verification>
1. On recipe detail page ([slug].vue), ingredients in the user's pantry show green "In Pantry" badge
2. Ingredients NOT in pantry show amber "Missing" badge with highlighted row
3. Tapping a missing ingredient on an AI-generated recipe opens the substitution dialog
4. On generate page result, each ingredient shows have/missing status
5. Generate result shows "X of Y ingredients" summary
6. Existing checklist toggle and swap icon work as before
7. No TypeScript errors
</verification>

<success_criteria>
- IngredientChecklist visually distinguishes pantry vs missing ingredients with color-coded badges
- Generate result page shows ingredient availability at a glance
- Missing ingredient tap triggers substitution dialog on AI recipes
- No regressions to existing checklist or substitution functionality
</success_criteria>

<output>
After completion, create `.planning/phases/17-ingredient-highlighting/17-01-SUMMARY.md`
</output>
