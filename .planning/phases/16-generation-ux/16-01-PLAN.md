---
phase: 16-generation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/composables/useGenerate.ts
  - app/components/GenerationProgress.vue
  - app/pages/generate.vue
autonomous: true
requirements:
  - GENX-01
  - GENX-02
  - GENX-03

must_haves:
  truths:
    - "During generation, users see three named steps (Crafting recipe, Validating ingredients, Generating photo) with the active step visually highlighted"
    - "Each step transition plays a distinct visual animation — not just a text swap"
    - "Each active step shows a per-step countdown (e.g., '~8s remaining') that ticks down"
    - "Completed steps show a green checkmark with a scale-in animation"
  artifacts:
    - path: "app/components/GenerationProgress.vue"
      provides: "Multi-step progress UI with per-step animations and countdown timers"
      min_lines: 80
    - path: "app/composables/useGenerate.ts"
      provides: "Generation state with simulated step transitions during generation"
      contains: "imaging"
  key_links:
    - from: "app/composables/useGenerate.ts"
      to: "app/components/GenerationProgress.vue"
      via: "status ref drives step highlighting and countdown"
      pattern: "status.*generating|validating|imaging"
    - from: "app/pages/generate.vue"
      to: "app/components/GenerationProgress.vue"
      via: "LazyGenerationProgress with status/startTime props"
      pattern: "LazyGenerationProgress"
---

<objective>
Build an engaging multi-step generation progress experience with distinct animations per step and per-step countdown timers.

Purpose: Recipe generation currently shows a basic progress list with minimal animation. This plan makes the generation feel alive — users see named steps with visual flair and know exactly how long each step will take.

Output: Enhanced GenerationProgress.vue with CSS animations and per-step countdown, updated useGenerate.ts with simulated step transitions.
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/composables/useGenerate.ts
@app/components/GenerationProgress.vue
@app/pages/generate.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add simulated step transitions to useGenerate composable and enhance GenerationProgress with animations and per-step countdowns</name>
  <files>
    app/composables/useGenerate.ts
    app/components/GenerationProgress.vue
    app/pages/generate.vue
  </files>
  <action>
**useGenerate.ts changes:**

The actual API call (`POST /api/recipes/generate`) is synchronous — it returns the full recipe in one response. The status currently jumps from `generating` straight to `complete`. To show multi-step progress, simulate step transitions on the client during the await:

1. In `generate()`, BEFORE the `$fetch` call, set `status.value = 'generating'` and `startTime.value = Date.now()`.
2. Start a step progression timer that transitions status through the pipeline:
   - `generating` at 0s (already set)
   - `validating` after ~5s
   - `imaging` after ~12s (only if the API hasn't resolved yet)
3. Use `setTimeout` with cleanup. When the API call resolves (success or error), cancel any pending timers so status doesn't advance after completion.
4. On success, set `status.value = 'complete'` (this already happens). On error, set `status.value = 'error'`.
5. Store the step transition timestamps in a reactive ref `stepStartTimes: Record<string, number>` so GenerationProgress can compute per-step elapsed time.

Key: The step transitions are SIMULATED on the client side to match roughly what the server is doing. The actual generation takes ~15-25s total. The timers should be cancelled immediately when the real response arrives.

Also update `generate.vue`: pass `stepStartTimes` as a prop to `LazyGenerationProgress` alongside existing props. Add `:step-start-times="stepStartTimes"` to the component usage.

**GenerationProgress.vue changes:**

Rebuild the component with these enhancements:

1. **Props:** Add `stepStartTimes` prop (`Record<string, number>`).

2. **Per-step countdown timer (GENX-03):**
   - Define step time estimates: `{ generating: 8, validating: 5, imaging: 12 }`.
   - For the ACTIVE step, compute remaining time as `estimate - (now - stepStartTimes[stepKey])` in seconds, updated every 500ms via an interval.
   - Display as "~Xs remaining" below the active step label. When remaining <= 0, show "Almost done...".
   - Only the active step shows the countdown — pending and completed steps do not.

3. **Distinct visual animations per step (GENX-02):**
   - **Active "generating" step:** Show a pulsing sparkles/magic-wand icon with a CSS `@keyframes sparkle` animation — scale 1 to 1.2 with opacity cycling, using amber/orange colors.
   - **Active "validating" step:** Show a rotating checkmark-shield icon with a CSS `@keyframes spin-slow` animation (one full rotation per 2s), using blue colors.
   - **Active "imaging" step:** Show a camera/photo icon with a CSS `@keyframes flash` animation — brief brightness pulse every 1.5s, using purple colors.
   - **Completed steps:** Green checkmark icon that appears with a `@keyframes scale-in` animation (scale 0 to 1 over 300ms with overshoot via cubic-bezier).
   - **Pending steps:** Gray circle with dashed border, no animation.

4. **Step label styling:**
   - Active: bold, dark text, slightly larger (text-base vs text-sm), with the step's accent color.
   - Completed: normal weight, green text, strikethrough-like muted style.
   - Pending: light gray text.

5. **Overall progress bar (optional enhancement):**
   - Add a thin horizontal progress bar at the top that fills based on which step is active: generating = 0-33%, validating = 33-66%, imaging = 66-100%, complete = 100%.
   - Use `transition: width 500ms ease-out` for smooth fill animation.

6. **CSS:** Use scoped `<style>` block with the keyframe animations. Do NOT use external animation libraries — pure CSS keyframes + Tailwind utilities.

Template structure (rough):
```
<div class="space-y-6">
  <!-- Progress bar -->
  <div class="h-1.5 bg-gray-200 rounded-full overflow-hidden">
    <div class="h-full bg-gradient-to-r from-amber-500 via-blue-500 to-purple-500 rounded-full transition-all duration-500" :style="{ width: progressPercent + '%' }" />
  </div>

  <!-- Steps -->
  <div v-for="step in steps" class="flex items-start gap-4 py-2">
    <!-- Animated icon area (40x40) -->
    <!-- Label + countdown -->
  </div>

  <!-- Error display (unchanged) -->
</div>
```
  </action>
  <verify>
    Run `npx nuxi build 2>&1 | tail -20` to confirm the build succeeds with no TypeScript errors. Verify the component renders by checking that GenerationProgress.vue has the three step animations defined in a scoped style block, and that useGenerate.ts exports `stepStartTimes`.
  </verify>
  <done>
    - GenerationProgress.vue shows 3 named steps with distinct per-step CSS animations (sparkle, spin, flash)
    - Active step displays a per-step countdown ("~Xs remaining") that ticks down from the step's estimate
    - Completed steps animate in with a scale-in green checkmark
    - useGenerate.ts simulates step transitions during the API await with proper cleanup on resolution
    - generate.vue passes stepStartTimes to GenerationProgress
    - Build compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx nuxi build` completes without TypeScript errors
2. GenerationProgress.vue contains `@keyframes sparkle`, `@keyframes spin-slow`, `@keyframes flash`, and `@keyframes scale-in` in scoped styles
3. useGenerate.ts has `stepStartTimes` in its return object
4. generate.vue passes `:step-start-times` prop to LazyGenerationProgress
5. Step time estimates exist for all 3 active steps (generating, validating, imaging)
</verification>

<success_criteria>
- During generation, the UI shows three named steps with the active one highlighted using a distinct animated icon
- Each step has its own CSS keyframe animation (not the same pulse on all steps)
- The active step displays an estimated time remaining that counts down
- Completed steps show a green checkmark with a scale-in entrance animation
- A progress bar at the top visually indicates overall progress
</success_criteria>

<output>
After completion, create `.planning/phases/16-generation-ux/16-01-SUMMARY.md`
</output>
