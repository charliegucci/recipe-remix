---
phase: 02-core-read-path
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - app/components/FeaturedCarousel.vue
  - app/components/RecipeCategorySection.vue
  - app/pages/index.vue
autonomous: true

must_haves:
  truths:
    - "User sees featured recipes in a carousel at the top"
    - "User sees recipes organized by cuisine category"
    - "User can scroll to load more recipes in each category"
    - "Page loads with content visible (no spinner for initial data)"
    - "Unauthenticated users see login/register CTAs in the header"
  artifacts:
    - path: "app/components/FeaturedCarousel.vue"
      provides: "Hero carousel for featured recipes"
      min_lines: 40
    - path: "app/components/RecipeCategorySection.vue"
      provides: "Category section with infinite scroll"
      min_lines: 50
    - path: "app/pages/index.vue"
      provides: "Home page with SSR data fetching"
      min_lines: 60
  key_links:
    - from: "app/pages/index.vue"
      to: "/api/recipes"
      via: "useAsyncData SSR fetch"
      pattern: "useAsyncData|useFetch"
    - from: "app/components/RecipeCategorySection.vue"
      to: "@vueuse/core"
      via: "useInfiniteScroll"
      pattern: "useInfiniteScroll"
---

<objective>
Build the home page with featured carousel and category sections with infinite scroll.

Purpose: The home page is the primary entry point for recipe discovery. Featured recipes in a carousel highlight curated picks. Category sections organized by cuisine let users browse by preference. Infinite scroll enables continuous discovery without pagination UI.

Output: FeaturedCarousel, RecipeCategorySection components, and fully wired index.vue home page.
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-read-path/02-RESEARCH.md
@.planning/phases/02-core-read-path/02-CONTEXT.md
@.planning/phases/02-core-read-path/02-03-PLAN.md
@app/components/AppHeader.vue
@app/pages/index.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeaturedCarousel component</name>
  <files>
    app/components/FeaturedCarousel.vue
  </files>
  <action>
Create a simple hand-rolled carousel for 3-5 featured recipes per RESEARCH.md recommendation:

**Props:**
```typescript
defineProps<{
  recipes: Array<{
    id: string
    title: string
    description: string
    cookTime: number
    imageKey: string | null
    cuisineTags: string[]
  }>
}>()
```

**Layout:**
- Full-width hero section at top of page
- Single visible slide at a time with large image
- Title and description overlay on image (bottom gradient)
- Navigation dots below carousel
- Optional: left/right arrows on desktop

**Implementation (no external library):**
- CSS flexbox with overflow-hidden
- Transform translateX for slide positioning
- `currentIndex` ref to track active slide
- Dot indicators show/change current slide
- Auto-advance every 5 seconds (optional, can skip for v1)

**Mobile touch:**
- Basic touch-swipe with pointer events (optional for v1)
- Or just rely on dots for navigation

**Styling:**
- Large image (aspect-[16/9] or aspect-[21/9] for cinematic)
- Gradient overlay from transparent to black at bottom
- White text for title/description on gradient
- Rounded corners on container
- Dot indicators: small circles, filled for active

Keep implementation simple - 3-5 slides doesn't need complex virtualization or lazy loading.
  </action>
  <verify>
Render FeaturedCarousel with 5 mock recipes. Test:
- Shows first slide on load
- Clicking dots changes slide
- Title and description are readable over image
- Works on mobile viewport
  </verify>
  <done>
FeaturedCarousel displays featured recipes with navigation. Slides transition smoothly. Works without external carousel library.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RecipeCategorySection with infinite scroll</name>
  <files>
    app/components/RecipeCategorySection.vue
  </files>
  <action>
Create category section with infinite scroll per CONTEXT.md and RESEARCH.md:

**Props:**
```typescript
defineProps<{
  category: string
  categoryLabel: string  // Display name (e.g., "Italian Cuisine")
  initialRecipes: Recipe[]
}>()
```

**Layout:**
- Section header with category name (h2)
- Grid of RecipeCards (1 col mobile, 2 col sm, 3 col lg per RESEARCH.md)
- Loading indicator at bottom when fetching more

**Infinite Scroll:**
- Import `useInfiniteScroll` from @vueuse/core
- Track `page` ref starting at 1
- Track `hasMore` ref starting at true
- Track `loading` ref for UI feedback
- Use `useTemplateRef` to get reference to section element (or use window)

**Fetch Logic:**
- On scroll trigger, increment page
- `$fetch('/api/recipes?category=${category}&page=${page}')`
- If response is empty array, set hasMore = false
- Otherwise, append to recipes array

**Pitfall 5 Prevention (from RESEARCH.md):**
- Set `canLoadMore` to check `hasMore.value && !loading.value`
- Don't fire immediately on mount - add mounted flag if needed

**Empty State:**
- If initialRecipes is empty and no more to load, show "No recipes in this category"
- Per CONTEXT.md: "Cross-promote on empty category" - show message suggesting other categories

**Loading State:**
- Show skeleton cards or spinner at bottom while loading more
- Could show 3 RecipeCardSkeleton components
  </action>
  <verify>
Render RecipeCategorySection with initial data. Test:
- Initial recipes display in grid
- Scrolling to bottom triggers fetch
- New recipes append to list
- Loading stops when no more data
- Does NOT fire fetch immediately on mount (Pitfall 5)
  </verify>
  <done>
RecipeCategorySection displays recipes in responsive grid. Infinite scroll loads more pages. Loading and empty states are handled.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire home page with SSR data fetching</name>
  <files>
    app/pages/index.vue
  </files>
  <action>
Replace placeholder index.vue with full recipe browsing experience.

**IMPORTANT - Auth Entry Points Preservation (Phase 1 Success Criterion 3):**
Before replacing index.vue, verify that AppHeader.vue handles auth CTAs for unauthenticated users:
- Read AppHeader.vue and confirm it shows "Sign In" and "Sign Up" buttons for unauthenticated users
- Confirm it shows "Create Account" prompt for guest/anonymous users
- The existing index.vue has auth prompts, but AppHeader already covers this case, so the new home page does NOT need to duplicate auth prompts in the page body

If AppHeader does NOT handle this, add a subtle banner component to the new index.vue for unauthenticated users.

**SSR Data Fetching:**
- Use `useAsyncData` to fetch all initial data in parallel:
  ```typescript
  const { data } = await useAsyncData('home', () =>
    Promise.all([
      $fetch('/api/recipes/featured'),
      $fetch('/api/recipes?category=italian&page=1'),
      $fetch('/api/recipes?category=mexican&page=1'),
      $fetch('/api/recipes?category=asian&page=1'),
      $fetch('/api/recipes?category=american&page=1'),
      $fetch('/api/recipes?category=mediterranean&page=1'),
    ])
  )
  ```
- Destructure results into named variables

**Page Layout:**
1. FeaturedCarousel with featured recipes
2. Spacer/padding
3. RecipeCategorySection for Italian
4. RecipeCategorySection for Mexican
5. RecipeCategorySection for Asian
6. RecipeCategorySection for American
7. RecipeCategorySection for Mediterranean

**Responsive Spacing:**
- Sections have vertical spacing (space-y-8 md:space-y-12)
- Container has horizontal padding (px-4 md:px-6 lg:px-8)
- Max-width container for desktop (max-w-7xl mx-auto)

**Error Handling:**
- If data fetch fails, show error state
- Individual sections can show empty state independently

**SEO:**
- Set page title via `useHead({ title: 'Recipe Remix - Discover Delicious Recipes' })`

This replaces the existing auth-landing index.vue content. The auth entry points are preserved via AppHeader which already shows login/register CTAs for unauthenticated users.
  </action>
  <verify>
Run dev server and visit home page:
- Featured carousel shows at top
- All 5 category sections display with initial recipes
- Scrolling category section loads more
- Page loads with content (no spinner during SSR)
- Check Network tab: initial data is in HTML payload, not separate XHR
- **Auth preservation check:** When logged out, AppHeader shows "Sign In" and "Sign Up" buttons
- **Auth preservation check:** When guest, AppHeader shows "Create Account" prompt
  </verify>
  <done>
Home page displays featured carousel and all cuisine categories. SSR delivers content on first paint. Infinite scroll works in category sections. Auth CTAs remain accessible via AppHeader for unauthenticated users.
  </done>
</task>

</tasks>

<verification>
1. FeaturedCarousel shows 3-5 featured recipes with navigation
2. RecipeCategorySection displays responsive grid of RecipeCards
3. Infinite scroll loads additional pages without page reload
4. Home page renders all sections with SSR (no client-side loading spinner)
5. Empty categories show appropriate messaging
6. Page is responsive across mobile/tablet/desktop
7. Navigation to recipe detail page works from cards
8. Unauthenticated users still have clear path to login/register via AppHeader
</verification>

<success_criteria>
- Featured carousel at top with dot navigation
- 5 cuisine category sections (Italian, Mexican, Asian, American, Mediterranean)
- Each section has infinite scroll pagination
- SSR delivers initial content without loading states
- Responsive grid layout (1/2/3 columns based on viewport)
- Cards link to recipe detail pages
- Page feels fast and content-rich on first load
- Auth entry points preserved (no regression against Phase 1 Success Criterion 3)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-read-path/02-05-SUMMARY.md`
</output>
