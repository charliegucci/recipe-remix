---
phase: 02-core-read-path
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/IngredientChecklist.vue
  - app/components/StepCard.vue
autonomous: true

must_haves:
  truths:
    - "User can tap ingredients to mark them as gathered"
    - "User can tap steps to mark them as complete"
    - "Progress persists across page reloads"
    - "Progress persists when switching between recipes"
  artifacts:
    - path: "app/components/IngredientChecklist.vue"
      provides: "Interactive ingredient list with checkboxes"
      min_lines: 30
    - path: "app/components/StepCard.vue"
      provides: "Cooking step card with completion checkbox"
      min_lines: 25
  key_links:
    - from: "app/components/IngredientChecklist.vue"
      to: "localStorage"
      via: "state persistence"
      pattern: "localStorage"
    - from: "app/components/StepCard.vue"
      to: "localStorage"
      via: "state persistence"
      pattern: "localStorage"
---

<objective>
Create interactive ingredient checklist and step card components for recipe detail page.

Purpose: Users cooking from their phone need to track progress. Tappable checkboxes for ingredients (gathering phase) and steps (cooking phase) provide a hands-free experience. localStorage persistence ensures progress survives phone screen-off or accidental navigation.

Output: IngredientChecklist and StepCard Vue components with localStorage-backed state.
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-read-path/02-RESEARCH.md
@.planning/phases/02-core-read-path/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IngredientChecklist component</name>
  <files>
    app/components/IngredientChecklist.vue
  </files>
  <action>
Create interactive ingredient checklist per CONTEXT.md decisions:

**Props:**
```typescript
defineProps<{
  recipeId: string
  ingredients: Array<{
    name: string
    quantity: string
    unit: string
  }>
}>()
```

**State:**
- `checked: Ref<Record<number, boolean>>` - map of ingredient index to checked state
- localStorage key: `recipe:${recipeId}:ingredients`

**SSR Safety (CRITICAL per RESEARCH.md Pitfall 2):**
- Initialize `checked` as empty object `{}`
- Read from localStorage in `onMounted()` only
- Watch `checked` and write to localStorage on change

**Layout:**
- List of ingredients with checkbox on left
- Each row: checkbox | quantity unit name (e.g., "2 cups flour")
- Checked items get strikethrough and muted color
- Large tap targets for mobile (min-h-12 or py-3)

**Styling:**
- Clean list with subtle dividers between items
- Checkbox should be visually prominent (custom styled or large native)
- Use Tailwind's line-through for checked state

**Clear button (optional but helpful):**
- "Clear all" button to reset checklist
- Clears localStorage entry for this recipe
  </action>
  <verify>
Mount component with mock data. Test:
- Tapping ingredient toggles checkbox
- Refresh page - checked state persists
- Navigate away and back - state persists
- Test with different recipeId - each recipe has independent state
  </verify>
  <done>
IngredientChecklist renders list of ingredients with checkboxes. Checked state persists in localStorage. SSR-safe (no errors on server render).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StepCard component</name>
  <files>
    app/components/StepCard.vue
  </files>
  <action>
Create cooking step card per CONTEXT.md decisions (card-per-step layout):

**Props:**
```typescript
defineProps<{
  recipeId: string
  stepNumber: number
  totalSteps: number
  instruction: string
}>()
```

**State:**
- `completed: Ref<boolean>` - whether this step is done
- localStorage key: `recipe:${recipeId}:steps`
- Store as object: `{ [stepNumber]: boolean }`

**SSR Safety:**
- Same pattern as IngredientChecklist
- Initialize as false, read from localStorage in onMounted

**Layout (card-per-step per CONTEXT.md):**
- Card container with padding and border/shadow
- Step number badge (e.g., "Step 2 of 6") at top
- Instruction text (can be multi-line)
- Checkbox/checkmark at bottom or right side
- Completed state: muted background, checkmark visible

**Styling:**
- Each step is visually distinct as a card
- Step number uses accent color (orange or primary)
- Completed steps: lighter background, green checkmark
- Transition animation on completion

**Touch-friendly:**
- Entire card is tappable to toggle completion (not just checkbox)
- Visual feedback on tap (scale or color change)
  </action>
  <verify>
Mount component with mock data. Test:
- Tapping card toggles completion
- Step number displays correctly ("Step 2 of 6")
- Completed state has distinct visual treatment
- Refresh page - completion state persists
- Works across different recipes independently
  </verify>
  <done>
StepCard renders as distinct card with step number, instruction, and completion toggle. State persists in localStorage. Entire card is tappable.
  </done>
</task>

</tasks>

<verification>
1. IngredientChecklist renders list with checkboxes
2. IngredientChecklist persists checked state to localStorage
3. StepCard renders as distinct card with step number
4. StepCard persists completion state to localStorage
5. Both components are SSR-safe (no localStorage errors during SSR)
6. Both components have large tap targets for mobile
7. State is scoped per-recipe (different recipes have independent state)
</verification>

<success_criteria>
- IngredientChecklist shows tappable ingredient rows with quantities
- StepCard displays as card with step number badge and instruction
- Both components persist progress to localStorage
- Both components are SSR-safe (hydrate from localStorage on client only)
- Touch targets are appropriately sized for mobile use
- Visual feedback clearly shows checked/completed state
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-read-path/02-04-SUMMARY.md`
</output>
