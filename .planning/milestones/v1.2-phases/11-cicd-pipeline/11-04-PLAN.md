---
phase: 11-cicd-pipeline
plan: 04
type: execute
wave: 2
depends_on: [11-01, 11-02, 11-03]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All four GitHub Actions workflows are syntactically valid and triggered correctly"
    - "Deploy workflow has been verified to work with NuxtHub deploy"
    - "User has confirmed NUXT_HUB_PROJECT_KEY secret is configured in GitHub"
  artifacts: []
  key_links: []
---

<objective>
Verify all four GitHub Actions workflows are correctly configured and guide the user through any required secret setup.

Purpose: The three prior plans fixed the workflow files, but we need to verify the complete picture: secrets are configured, triggers are correct, and the user understands how to test the workflows end-to-end.

Output: Verified workflow configuration and user confirmation of secret setup.
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.github/workflows/deploy.yml
@.github/workflows/preview.yml
@.github/workflows/ci.yml
@.github/workflows/smoke-tests.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate all workflow files and check for consistency</name>
  <files></files>
  <action>
Run validation checks on all four workflow files:

1. Validate YAML syntax for each workflow:
   - `.github/workflows/deploy.yml`
   - `.github/workflows/preview.yml`
   - `.github/workflows/ci.yml`
   - `.github/workflows/smoke-tests.yml`

2. Verify trigger configuration:
   - deploy.yml: `on: push: branches: [main]`
   - preview.yml: `on: pull_request: branches: [main]`
   - ci.yml: `on: pull_request: branches: [main]`
   - smoke-tests.yml: `on: workflow_run: workflows: ["Deploy to Production"]`

3. Verify the workflow name in deploy.yml matches exactly what smoke-tests.yml references in its `workflow_run.workflows` array. If deploy.yml has `name: Deploy to Production`, smoke-tests.yml must reference `"Deploy to Production"` exactly.

4. Check that all referenced secrets are consistent:
   - `NUXT_HUB_PROJECT_KEY` in deploy.yml and preview.yml
   - `PRODUCTION_URL` in smoke-tests.yml (with fallback)

5. Print a summary of all workflows, their triggers, and required secrets.
  </action>
  <verify>All four workflow files are valid YAML with correct triggers and consistent secret references</verify>
  <done>All workflows validated for syntax, trigger correctness, and secret consistency</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify workflows end-to-end and configure secrets</name>
  <files></files>
  <action>
All four GitHub Actions workflows have been fixed:
1. Deploy to Production — now uses `npx nuxthub deploy --production --no-build`
2. Deploy Preview — now uses `npx nuxthub deploy --preview --no-build` and posts preview URL
3. CI Gates — bundle size checks `.output/server/` gzip size, Lighthouse runs with lighthouserc.cjs
4. Smoke Tests — resilient selectors, production URL fallback to remix-recipe.com

Required setup:
1. Generate NuxtHub project deploy key via `npx nuxthub link` or NuxtHub dashboard
2. Add GitHub secrets: `NUXT_HUB_PROJECT_KEY` and optionally `PRODUCTION_URL`
3. Push to main to trigger deploy, create PR to trigger preview + CI gates
  </action>
  <verify>
Visit https://github.com/{owner}/{repo}/actions and confirm:
- Deploy to Production completes successfully on push to main
- Deploy Preview posts preview URL comment on PRs
- CI Gates (bundle-size + lighthouse) run on PRs
- Smoke Tests trigger after successful deploy
  </verify>
  <done>All four workflows run successfully in GitHub Actions with proper triggers and results</done>
</task>

</tasks>

<verification>
1. All four workflow files exist and are valid YAML
2. Trigger chain is correct: push->deploy->smoke-tests, PR->preview+ci
3. User has confirmed secrets are configured
</verification>

<success_criteria>
- All workflows validated for syntax and configuration consistency
- User has been guided through required secret setup (NUXT_HUB_PROJECT_KEY)
- User can verify workflows by pushing to main and creating a PR
</success_criteria>

<output>
After completion, create `.planning/phases/11-cicd-pipeline/11-04-SUMMARY.md`
</output>
