---
phase: 18-favorites-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/favorites.vue
autonomous: true
requirements:
  - FAV-02
  - FAV-03

must_haves:
  truths:
    - "Favorites page lists all saved recipes as recipe cards with titles and images"
    - "User can remove a recipe from the Favorites page without navigating to the recipe detail"
    - "After removing, the card disappears from the grid with a fade-out transition"
    - "Empty state appears when the last recipe is removed"
  artifacts:
    - path: "app/pages/favorites.vue"
      provides: "Favorites page with recipe card grid and inline remove"
      contains: "toggleFavorite"
  key_links:
    - from: "app/pages/favorites.vue"
      to: "app/composables/useFavorites.ts"
      via: "composable for remove action and state sync"
      pattern: "useFavorites"
    - from: "app/pages/favorites.vue"
      to: "app/components/RecipeCard.vue"
      via: "recipe card rendering with images"
      pattern: "RecipeCard"
---

<objective>
Enhance the Favorites page with inline remove functionality so users can manage their collection without navigating to individual recipes.

Purpose: The favorites page already renders recipe cards (FAV-02 partially met), but lacks any way to remove a recipe without clicking into the card's tiny heart overlay (FAV-03 unmet). This plan adds a visible "Remove" button on each card in the favorites grid and handles the removal with optimistic UI + transition.

Output: Favorites page with recipe cards showing images/titles and an inline remove button per card.
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/pages/favorites.vue
@app/composables/useFavorites.ts
@app/components/RecipeCard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline remove button and optimistic removal to Favorites page</name>
  <files>app/pages/favorites.vue</files>
  <action>
1. **Import and use `useFavorites` composable** in favorites.vue:
   - Add `const { toggleFavorite } = useFavorites()` to get the remove action.
   - This reuses the existing optimistic toggle logic (removes from favoriteIds Set, calls DELETE API, reverts on error).

2. **Add an inline remove button** to each recipe card in the grid:
   - Wrap each RecipeCard in a `<div class="relative group">`.
   - Add a remove button positioned at the top-right of each card wrapper: `<button class="absolute top-3 right-3 z-20 ...">`
   - Button style: `bg-white/90 backdrop-blur-sm rounded-full p-2 shadow-sm hover:bg-red-50 hover:text-red-600 transition-all min-w-[44px] min-h-[44px] flex items-center justify-center` with an X icon (or trash icon).
   - Use a small X SVG icon (`w-4 h-4`) inside the button.
   - aria-label: "Remove from favorites"
   - On click: `@click.prevent.stop="removeRecipe(recipe.id)"` — prevent navigation to recipe detail.

3. **Implement removeRecipe function:**
   ```typescript
   async function removeRecipe(recipeId: string) {
     await toggleFavorite(recipeId)
     // Refresh the favorites list from server to stay in sync
     await refresh()
   }
   ```

4. **Add Vue transition-group** for smooth removal animation:
   - Replace the plain `<div class="grid ...">` with `<TransitionGroup name="favorites-fade" tag="div" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">`.
   - Add scoped CSS for the transition:
     ```css
     .favorites-fade-leave-active {
       transition: all 0.3s ease;
     }
     .favorites-fade-leave-to {
       opacity: 0;
       transform: scale(0.95);
     }
     ```
   - Ensure each card has `:key="recipe.id"` (already present).

5. **Handle empty state after last removal:**
   - The existing `v-else-if="recipes.length === 0"` empty state already handles this — after `refresh()` returns an empty list, the empty state will render automatically.

6. **Verify existing RecipeCard image display:**
   - RecipeCard already handles imageKey -> `/api/images/{key}` URL resolution and gradient fallback. No changes needed for FAV-02 (images display correctly).

Do NOT modify RecipeCard.vue, useFavorites.ts, or any API routes — they already provide all needed functionality.
  </action>
  <verify>
Run `npx nuxi typecheck` to confirm no TypeScript errors. Run `npx nuxi build` to verify build succeeds. Visually: favorites page should show recipe cards with images, each having a remove button at top-right, and removing should fade the card out.
  </verify>
  <done>
Favorites page lists saved recipes as cards with titles and images. Each card has a visible "Remove" button (X icon) at top-right. Clicking remove fades the card out, calls the DELETE API via useFavorites, and refreshes the list. When all recipes are removed, the empty state renders.
  </done>
</task>

</tasks>

<verification>
1. Navigate to /favorites while logged in with saved favorites — recipe cards render with images and titles
2. Each card has a visible remove button (X) at top-right
3. Click the X — card fades out and disappears from the grid
4. The server-side favorite is actually deleted (refresh page — card is still gone)
5. Remove all favorites — empty state appears with "No favorites yet" message
6. RecipeCard's existing small heart overlay still toggles correctly on the favorites page
</verification>

<success_criteria>
- Favorites page shows recipe cards with images and titles (FAV-02)
- Each card has an inline remove button that doesn't require navigating away (FAV-03)
- Removal is optimistic with fade-out animation
- Empty state renders when all favorites removed
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-favorites-polish/18-02-SUMMARY.md`
</output>
