---
phase: 18-favorites-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/recipe/[slug].vue
  - app/components/FavoriteButton.vue
autonomous: true
requirements:
  - FAV-01
  - FAV-04

must_haves:
  truths:
    - "Recipe detail page shows a clearly visible heart CTA below the hero image that toggles save/remove"
    - "Tapping the heart CTA produces immediate optimistic UI feedback (filled/unfilled toggle without waiting for server)"
    - "Favorite state persists across page refresh for logged-in users"
    - "Anonymous/guest users tapping the heart CTA are redirected to login"
  artifacts:
    - path: "app/pages/recipe/[slug].vue"
      provides: "Prominent FavoriteButton on recipe detail page"
      contains: "FavoriteButton"
    - path: "app/components/FavoriteButton.vue"
      provides: "Heart toggle CTA with size variants"
      contains: "toggleFavorite"
  key_links:
    - from: "app/pages/recipe/[slug].vue"
      to: "app/components/FavoriteButton.vue"
      via: "component usage with recipe ID prop"
      pattern: "FavoriteButton.*recipe-id"
    - from: "app/components/FavoriteButton.vue"
      to: "app/composables/useFavorites.ts"
      via: "composable for toggle and state"
      pattern: "useFavorites"
---

<objective>
Add a prominent favorite heart CTA to the recipe detail page and enhance FavoriteButton with a larger variant for detail-page use.

Purpose: FAV-01 requires a "clearly visible" save/remove CTA on recipe detail — currently FavoriteButton only appears as a small overlay on RecipeCard. The detail page has no favorite CTA at all. FAV-04 (server persistence) already works via the existing useFavorites composable with optimistic UI.

Output: FavoriteButton with size prop (sm/lg), recipe detail page with prominent heart CTA in the metadata bar.
</objective>

<execution_context>
@/Users/wilsonesmundo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilsonesmundo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/pages/recipe/[slug].vue
@app/components/FavoriteButton.vue
@app/composables/useFavorites.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add size variant to FavoriteButton and place prominent CTA on recipe detail page</name>
  <files>app/components/FavoriteButton.vue, app/pages/recipe/[slug].vue</files>
  <action>
1. **FavoriteButton.vue** — Add an optional `size` prop (default `'sm'`):
   - `sm` (current behavior): `p-2`, `w-5 h-5` icons, `bg-white/90` pill — used on RecipeCard overlay
   - `lg`: `px-4 py-2.5`, `w-6 h-6` icons, text label "Save" / "Saved", `bg-white` with `border border-gray-200 shadow-sm` — used on recipe detail page
   - For `lg` size: show text next to heart: "Save" when not favorited, "Saved" when favorited. Use `text-sm font-medium` for the label.
   - Add a brief scale animation on toggle: `motion-safe:active:scale-95` and a `transition-all duration-200` on the button.
   - When `lg` and favorited: button gets a subtle `bg-red-50 border-red-200` background to reinforce saved state.
   - Keep all existing functionality (event.stopPropagation, requiresAuth redirect, optimistic toggle, aria-label, 44px touch target).

2. **[slug].vue** — Add FavoriteButton to the metadata bar:
   - In the metadata bar section (the `<div class="bg-white rounded-lg p-6 mb-6 shadow-sm">` after the "Back to Home" link), add FavoriteButton at the end of the flex container that holds rating and metadata badges.
   - Place it inside a new flex row or alongside existing badges: `<FavoriteButton :recipe-id="recipeId" size="lg" />` wrapped in a client-only guard (`<ClientOnly>`).
   - Position: right-aligned in the metadata bar, next to the cuisine/dietary tags row. Use `ml-auto` or a flex justify-between wrapper so it sits on the right side of the badge row.
   - Ensure it renders only on client (wrap in `<ClientOnly>` or use the existing `mounted` guard pattern from RecipeCard).

Do NOT modify useFavorites.ts — it already handles optimistic toggle, auth gating, and server sync correctly.
  </action>
  <verify>
Run `npx nuxi typecheck` to confirm no TypeScript errors. Run `npx nuxi build` to verify build succeeds. Visually: recipe detail page should show a "Save" / "Saved" button with heart icon in the metadata bar area.
  </verify>
  <done>
Recipe detail page at /recipe/{slug} has a clearly visible heart CTA that reads "Save"/"Saved", toggles optimistically on click, redirects anonymous users to /login, and persists state via the existing server-synced favorites API.
  </done>
</task>

</tasks>

<verification>
1. Navigate to any recipe detail page — a "Save" button with heart icon is visible in the metadata section
2. Click the heart — it toggles to filled red heart with "Saved" text and bg-red-50 styling (optimistic, no page reload)
3. Refresh the page — the "Saved" state persists (server-synced)
4. Log out / use guest account — clicking the heart redirects to /login
5. RecipeCard small heart overlay still works unchanged
</verification>

<success_criteria>
- FavoriteButton has `size` prop with `sm` (default, unchanged) and `lg` (text label + larger) variants
- Recipe detail page shows prominent favorite CTA in metadata bar
- Optimistic toggle works (instant visual feedback)
- Auth gate redirects anonymous users to login
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-favorites-polish/18-01-SUMMARY.md`
</output>
